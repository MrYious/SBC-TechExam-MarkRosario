import iconCross from '../assets/cross.svg'

export const NewRecipeButton = () => {

    return (
        <button onClick={()=>{}} className='newRecipeButton'>
            <img src={iconCross} alt="icon cross" />
        </button>
    )
}